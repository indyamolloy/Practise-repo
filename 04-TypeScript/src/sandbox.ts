let names: (string | number)[] = ["indya", "kieran", "ellie"];

names[0] = 12;

console.log(names);

let dog = {
  name: "doggy",
  cute: true,
};

let age: number;
//union types
let mixed: (string | number)[];

let obj: object;
